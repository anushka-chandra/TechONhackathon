/** @type {import('next').NextConfig} */
const nextConfig = {
  allowedDevOrigins: ['10.154.91.82'],
  // Hide the floating Next.js dev indicator (the red "N" badge in the corner)
  devIndicators: false,
};

module.exports = nextConfig;